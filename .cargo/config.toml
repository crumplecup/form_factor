# Note: LIBCLANG_PATH and LD_LIBRARY_PATH must be set in ~/.bashrc
# The [env] section below only applies to the final binary, not build scripts.
# Build scripts for opencv-sys and tesseract-sys run before any cargo config
# can help, so shell environment variables are required.
