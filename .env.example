# Form Factor Environment Configuration
# Copy this file to .env and customize as needed

# Logging configuration
# Set to one of: trace, debug, info, warn, error
# Can also target specific modules, e.g., form_factor::drawing=debug
# Examples:
#   RUST_LOG=info                              # Show info and above for all modules
#   RUST_LOG=form_factor=debug                 # Debug for entire app
#   RUST_LOG=form_factor::drawing=trace        # Trace for drawing module
#   RUST_LOG=form_factor::drawing[shape_name_autofocus]=debug  # Debug specific span
RUST_LOG=info

# LibClang configuration (required for OpenCV/Tesseract bindings)
# Set this to your system's libclang location:
#   Arch/Manjaro: /usr/lib
#   Ubuntu/Debian: /usr/lib/llvm-14/lib (or your LLVM version)
#   Fedora: /usr/lib64
LIBCLANG_PATH=/usr/lib
LD_LIBRARY_PATH=/usr/lib
